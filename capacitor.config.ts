import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplybest.thale.maytabs',
  appName: 'MTech Tabs',
  webDir: 'www',
  server: {
    /* optional url to try live-reload in emulators */
    // url: 'http://localhost:8100/',
  }
};

export default config;
